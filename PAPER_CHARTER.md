# Gripper-Aware VLA —— 论文定稿纲领(单一真源)

> 目标会议: **RSS 2027**(投稿截止通常在 1 月底–2 月初, 以官方 CFP 为准)。
> 本文件是核心主张与实验计划的唯一权威版本; 讨论以此为基准, 改主张先改这里。
> 采集/部署协议见 airbot/TASK_PROTOCOL.md; 机制实验结论的原始表格见 git 历史与 wandb。

## 一句话论点

现有 VLA(含 pi0.5)把夹爪当作机械臂的"第 7 个电机"——一个一自由度开合量。
我们把夹爪当作**独立的、有几何、有区域分工、有力学行为的实体**来建模,
得到一个 **gripper-aware** 的 VLA: 同一套权重跨爪 rollout、按任务选用夹爪区域、
按任务调节用力。**不做触觉传感器**(力来自电机电流)。

## 三根支柱

### 支柱 1 —— 跨爪(Cross-gripper): 换爪不换权重, 新爪只给点云
- **主张**: 现有模型换爪需要重新采集/标定; 我们用几何点云条件化 + 无 TCP 标定的
  臂参考系接口(arm-frame), 训练爪之间 parity, 未见爪零样本/少样本可用。
- **已有证据**: 两爪 parity(6 任务成对 50+50); 真机 A/B/A/B 模式切换(装平行爪喂 GET
  点云 → 下探深 ~21mm = 示教差, 无遮断权重无此现象); 视觉剥夺剂量-反应(swap 效应
  2.4→3.3→6.1); arm-frame 去标定管线已跑通(armframe_v1/v2)。
- **缺口(必做)**: ① 训练爪扩到 4 把(parallel / GET / UMI 式柔性宽爪 / 一把几何变体),
  同臂同任务同协议; ② **第 5 把爪(内插型)零样本 + 少样本阶梯 0→10→50 条**——本文
  headline 实验, 目前 0 证据; ③ vanilla pi0.5 同数据消融成功率表。
- **精确措辞**: 不是"pi0.5 不能开不同的爪", 而是"它把爪身份隐含在视觉/动作空间里,
  无法显式利用几何, 因而换新爪需要重训重标"。

### 支柱 2 —— 区域感知(Region-aware): 知道该用爪的哪个部位
- **主张**: 夹爪按功能分为 tip / mid / rear 三区(区域 token), 模型学会按任务选用区域
  (杯沿用 tip, 扫帚用 mid 握持, 推/扫用 rear 等)。
- **已有证据**: armframe_v1 探针中"区域乱序"成为最大效应(结构被读取); 区域 token
  管线完整。
- **缺口**: ① 至少三个任务分别以 tip / mid / rear 为主要接触区(现有: 杯沿=tip;
  需补 mid/rear 主导的任务, 如推物/搅拌/按压); ② 区域 vs 全局 token 的消融
  (成功率 + 探针); ③ 逐任务的区域归因可视化(FiLM/注意力对各区域 token 的依赖
  随任务阶段变化)。这是目前证据最薄的支柱。

### 支柱 3 —— 力感知(Force-aware): 知道该用多大力
- **主张**: 用关节/夹爪电机电流(力矩)作为接触信号, 模型学会按任务调节用力
  (切透、握稳不滑、扫帚压桌)。**不上触觉传感器。**
- **已有证据**: 100Hz 遥测 sidecar 自挂杯任务起全程记录(tau/dq/grip_tau);
  尚未进入网络。
- **计划**: 第一步 Level A——动作条件化前向预测辅助头(预测未来 chunk 的力矩曲线,
  遥测缺失的旧数据掩码), 产出表征塑形 + 部署监视器(预测 vs 实测失配 = 切透/滑移检测);
  第二步 Level B——力矩作为输入 token, 用同一探针方法学验证"接触通道被使用"
  (与几何通道形成一死一活对照)。NeuralActuator 电流→牛顿标定为可选增强, 不做门槛。
- **缺口**: 全部。验收指标: held-out 力矩预测相关性; 切断率/滑移率等接触子指标;
  探针倍数。

## 贯穿三支柱的科学主线(论文的脊柱, 不只是三个功能)

**条件化通道被使用的程度, 取决于它是否是该信息的唯一来源; 架构改不动, 信息结构才能。**
证据链(已完成): token 注入被注意力无视 → FiLM 结构注入被主动收缩 → 相机遮断(含全盲
样本)激活通道 → 去标定后区域结构被读取 + 视觉剥夺剂量-反应。预测: 力通道因视觉
不可见而自然活跃。探针方法学(probe_token_swap.py + 训练中在线探针)是本文的分析工具贡献。

## 实验矩阵

- 任务(7, 每爪 50 条, 同 prompt 跨爪配对): 积木入碗、金字塔、擦黑板、挂杯、
  切泥(仅 GET, 平行爪为能力边界评测项)、扫物入铲(双工具双臂)、抽屉分类整理。
- 爪: 训练 4 把 + 保留 1 把(零样本/少样本)。
- 本体: AIRBOT Play 主线; YAM 臂作为跨本体加分项(**同一批爪复刻同任务**, 与爪交叉
  而非对齐)。
- 评测分两桶: 分布内(训练任务/物体/爪)与分布外(保留物体、保留爪、新场景)。
- 消融梯: vanilla / 区域 token / +FiLM / +遮断 / +arm-frame / +force。

## 明确不做

触觉传感器; 从头训练(始终 pi0.5 初始化); 左右臂对齐式随机化; 语言指定夹爪身份
(所有跨爪对照同 prompt); 在训练集里放"某把爪做不到的任务"(那是评测项)。

## 里程碑(粗略)

1. 抽屉任务 GET/parallel 采完 → armframe_v2 评测(挂杯成功率 vs film_v1, 扫地零样本物体)
2. 深度/内参/rig.json 进采集脚本(带开关) → 第 3、4 把爪入库 + 采集
3. 力感知 Level A 管线(遥测对齐打包 + 辅助头 + 掩码 loss) → 训练 → 探针/子指标
4. 第 5 把爪零样本/少样本阶梯(headline)
5. 区域主导任务补齐 + 区域消融; 数据 ≥40h 时评估 LoRA→全参+共训
6. 写作: 机制主线 + 三支柱 + 消融表
